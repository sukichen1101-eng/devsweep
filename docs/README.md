# docs

Put a screen recording here as `demo.gif`, then it shows up in the main README:

```markdown
![demo](docs/demo.gif)
```

Recording tips: use ScreenToGif / LICEcap / ShareX. Show the scan → preview → execute flow.
